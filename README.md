# tmux

```bash
git clone https://github.com/vodyanoysh/tmux.git /tmp/tmux-config \
  && cp /tmp/tmux-config/.tmux.conf "$HOME"/.tmux.conf \
  && rm -rf /tmp/tmux-config
  && git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm
```
